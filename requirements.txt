# Requirements for Qwen3.5-4B Fine-Tuning with Unsloth
# -------------------------------------------------------
# Install in this order:
#
# Step 1 — PyTorch (CUDA 12.8):
#   uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# Step 2 — flash-attn and causal-conv1d (require --no-build-isolation):
#   uv pip install flash-attn causal-conv1d --no-build-isolation
#
# Step 3 — Everything else:
#   uv pip install -r requirements.txt
# -------------------------------------------------------

transformers
peft
trl
bitsandbytes
accelerate
qwen-vl-utils
pillow
datasets
sentencepiece
protobuf
jiwer
unsloth
flash-linear-attention