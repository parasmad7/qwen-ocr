# Requirements for Qwen3.5-4B Fine-Tuning with Unsloth & vLLM
# -------------------------------------------------------
# Install in this exact order:
#
# Step 1 — Install PyTorch FIRST (exact version vLLM 0.11.0 requires):
#   uv pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0 --index-url https://download.pytorch.org/whl/cu128
#
# Step 2 — Install all other requirements:
#   uv pip install -r requirements.txt
#
# Step 3 — Recompile CUDA extensions against torch 2.8.0 (order matters):
#   uv pip install causal-conv1d --no-build-isolation
#   uv pip install flash-attn --no-build-isolation
#   uv pip install flash-linear-attention --no-build-isolation
#
# Verify:
#   python -c "import causal_conv1d; print('causal_conv1d OK')"
#   python -c "import flash_attn; print('flash_attn OK')"
# -------------------------------------------------------

# Inference
vllm==0.11.0

# Core training
transformers
peft
trl
bitsandbytes
accelerate
unsloth

# Qwen vision-language
qwen-vl-utils
pillow
albumentations

# Data & tokenization
datasets
sentencepiece
protobuf

# Evaluation
jiwer

# Attention kernels (installed separately in Step 3 — do not add here)
# flash-attn
# causal-conv1d
# flash-linear-attention